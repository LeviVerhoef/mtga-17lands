# Core requirements — install with: pip install -r requirements.txt
# Works on macOS and Windows with Python 3.12–3.14.

numpy>=2.0
Pillow>=10.0
pydantic>=2.0
requests>=2.28
ttkbootstrap>=1.10
scipy>=1.11
duckdb>=1.1
httpx>=0.27

# Windows only (ignored automatically on other platforms)
pywin32>=306; sys_platform == "win32"
